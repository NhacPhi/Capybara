using Cysharp.Threading.Tasks;
using Observer;
using UnityEngine;
using VContainer;

namespace UI
{
    public class RewardPannel : PanelBase
    {
        protected override void OnAwake()
        {
            base.OnAwake();
        }

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }

}

